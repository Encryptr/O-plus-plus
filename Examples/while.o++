# O++ While example

var counter = NULL;

while counter < 10 {
	var i = counter * counter;

	echo(counter, "*", counter, "=", i);

	counter = counter + 1;
}

# Future?
var a = Vector{10,20};
a.dostuff();

Object Human {
	var name;
	var age;

	func dostuff() {
		age = age*age;
	}
};

